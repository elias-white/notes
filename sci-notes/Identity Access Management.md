In IAM, you define *who* (identity) has *what access* (role) for *which resource*.

In IAM, Permissions are not granted directly to users, rather, they are granted to *roles* in which users can be members of.